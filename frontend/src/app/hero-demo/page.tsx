import GhibliRobotHeroDemo from "@/components/ghibli-robot-hero-demo";

/** Isolated preview route for the GhibliRobotHero UI component. Not linked from
 *  the product — its bright, playful look is intentionally separate from the
 *  dark Synapse dashboard. */
export default function HeroDemoPage() {
  return <GhibliRobotHeroDemo />;
}
